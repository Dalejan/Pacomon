import { LifePointsDirective } from './life-points.directive';

describe('LifePointsDirective', () => {
  it('should create an instance', () => {
    const directive = new LifePointsDirective();
    expect(directive).toBeTruthy();
  });
});
